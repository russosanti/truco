# CLAUDE.md — dev log & decisions

> Temporary working notes for the PRD implementation. **Delete before final delivery** (along with `*_test.lua` and the scratchpad harnesses).

## Toolchain

- **Run unit tests with `luajit`, not `lua`.** Homebrew's `lua` is 5.5, which makes `for` control variables `const` and breaks the vendored `lib/class.lua` (`k = include_helper(...)`). LÖVE embeds LuaJIT (5.1 semantics), so `luajit` is the correct match.
  - `luajit lib/knife/test.lua src/<file>_test.lua`
- LÖVE 11.5 installed via `brew install --cask love` (cask is deprecated/Gatekeeper-flagged but the 11.5 binary works). Run: `love .`

## PRD 1 — card & deck model

- `Card` / `Deck` / `card_defs` live flat under `src/` (Dependencies.lua already scaffolded flat require paths; PRD's `src/truco/` was optional).
- Trick tiers, envido values, sprite-quad index all resolved from `card_defs` lookup tables.

## PRD 2 — hand loop

- Phases sequenced by an internal `StateMachine` inside `HandLoopState`: deal → cantos (empty hook) → trick → score → deal.
- **`isHandDecided` — prose vs. table reconciliation.** PRD §5 prose only decides on a tie "if the trick that just finished was a tie," which does NOT settle the `tie → P1 wins` row after trick 2 as the worked-examples table requires. Fix: the number of pardas so far is inferable as `tricksPlayed - wins.human - wins.ai`, so "a parda occurred AND one side leads on clean wins → decide now" closes the gap. See comment in `src/trick_rules.lua`.
- **PRD 2 §5's table was WRONG on one row (user, found in play).** It gives a 1-1 hand whose third trick is a parda to *mano*; the reglamento gives it to whoever won **trick 1**. Mano decides only the all-parda hand. Found by playing win/lose/parda as non-mano and being awarded the loss; confirmed by enumerating all 27 trick sequences × both manos against the rule stated declaratively — **exactly 2 of the 54 rows disagreed** (the row and its mirror). It hid because when mano happens to be the trick-1 winner the wrong rule still gives the right answer.
  - The whole reglamento is one sentence: **the winner of the earliest non-parda trick takes the hand; mano takes it only if all three are pardas.** The existing "parda + a lead on clean wins" branch already covers every case where the earliest non-parda isn't trick 1, so the only gap was equal-wins-at-3-tricks — and there the parda can only *be* the third (an earlier one alongside unequal wins ends the hand at trick 2), so trick 1 decides. Fallback is now `firstTrickWinner or mano`.
  - `firstTrickWinner` **replaced the vestigial `lastTrickWasTie` param** (dead since PRD 2) rather than being appended — arity stays 4, and there were only two callers. `TrickState` tracks it per hand in `init`, set in `resolve` when `tricksPlayed == 1`; `winnerSide` is already nil on a parda, which is exactly the "nobody yet" value the rule wants.
  - `trick_rules_test.lua` now asserts the row under **both** manos (so mano can't be what decides it) plus an exhaustive 54-row cross-check against the declarative reference — two independent formulations agreeing on the winner *and* the trick it's known on. That cross-check is what located the bug. No AI change needed: `AiStub`'s tie-over-loss preference is already correct in both directions under the fixed rule.
- **AI think delay:** AI move scheduled via `Timer.after(0.6, ...)` behind an `aiThinking` one-shot guard, so its card doesn't land on the same frame as the human's.

## PRD 2 addendum — trick rendering polish

- **Play animation:** each played card is a `{card,x,y,rot,done}` object tweened (`Timer.tween`, 0.2s) from its hand slot to its table slot with a ±7° random settle tilt. `drawCardFrontRot` (in `table_render.lua`) rotates about the card center.
- **Resolve gating:** `resolve()` is deferred until BOTH cards' tweens finish, via `tryResolve()` (called from each tween's `:finish`).
- **Input gating (gotcha):** deferring resolve opened a window where both cards are down (`playCount==2`) but `resolving` is still false and `currentPlayer` has flipped back — `update` happily accepted a *third* card, draining the hand and soft-locking. Fixed with an explicit `if self.playCount >= 2 then return end` guard in `update` (turn-flipping alone was NOT enough, contrary to the first plan). Caught by the headless smoke harness.
- **Played-card position (item 3):** Y-only split — AI up (`AI_PLAYED_Y=60`), human down (`HUMAN_PLAYED_Y=92`), ~18px overlap; X unchanged (`cardRowX(1,2)` / `cardRowX(2,2)`).
- **Item 4 decision:** removed BOTH the "AI" and "You" played-slot labels (and the empty-slot outline rects), not just "You" — with item 3's positioning, owner is conveyed by position, and the label/outline block under the AI hand was what read as a stray face-down "You" row. Top area is now strictly the AI's 3 backs.
- **Asset swap (item 1):** alpha-correct `graphics/deck_sheet.png` / `card_back.png` are a user-provided drop-in; no code change (same grid/cells/quad formula). Code draws at full opacity, so transparent corners render rounded automatically.
- **Human input (replaced 1/2/3):** raise-only picker, no highlight ring — the lifted card (`HUMAN_HAND_RAISE=12`) is the only cue. State is a **nilable** `self.raised` (index or nil; nothing raised at trick start) plus `self.prevHovered`. Mouse drives the lift on *transitions* only: entering a card raises it; leaving a card it was over lowers it; moving through empty space (hovered nil→nil) leaves a keyboard-raised card alone (the one non-obvious rule — the only mouse-driven lower is the `hovered==nil and prevHovered~=nil` exit edge). Arrows lift card 1 when none is up, else move ±1. Enter plays the raised card (no-op if none); left-click plays the card under the mouse (`cardAtMouse` via `push.toGame`, hit band spans raised+resting Y; `mouseWasDown` edge detection). Verified headless in `scratchpad/input.lua` (arrow-lifts-first, enter/exit, empty-space keeps lift, Enter-no-op, click). `smoke.lua`/`rotation.lua` drive with Right+Enter (Right lifts card 1, Enter plays it) with the mouse stubbed outside the game area (`push.toGame` overridden).
- **Asset alpha bug + fix:** the dropped-in PNGs had a broken alpha channel — the card's white *background* was keyed to transparent across the right ~35% of every cell (only ink/border stayed opaque), so the green table showed through and the art appeared to float. The underlying RGB there was already white. Fixed programmatically (PIL): rebuilt each cell's alpha from a rounded-rectangle mask (130×200, radius 6) — opaque card body, only the true outer corners transparent. Applied to all 40 cells + `card_back.png`; originals backed up in scratchpad (`deck_sheet.ORIG.png`, `card_back.ORIG.png`). Same dimensions/format (1300×800 RGBA). Verified by compositing cells over the table green and eyeballing.

## PRD 3 — envido

- **Pure math** in `src/envido_rules.lua` (`envidoValue`/`envidoWinner`/`rejectValue`/`faltaEnvidoValue`); throwaway `EnvidoAiStub` (open if ≥27, quiero if ≥24, never raises).
- **Falta modeling (not spelled out in the PRD):** `cumulative` tracks only envido(2)/real(3); falta is a ceiling that adds nothing. Accept-falta pays `faltaEnvidoValue` (ignores cumulative); reject-falta pays `max(cumulative,1)` = the pre-falta pot floored at 1 (direct falta→1, Envido→Falta rejected→2). Non-falta reject uses `rejectValue(cumulative,lastCallValue)`.
- **Reusable call engine:** `src/canto.lua` is a family-agnostic call/response/escalation state machine (`Canto`), config-driven — tracks calls/cumulative/caller/responder and yields a structured `outcome`. `src/envido_canto.lua` binds it to envido: the `ENVIDO_CANTO` ladder config + `envidoAward(outcome, mano, ...)` → (side, points) via envido_rules. Tested in `src/canto_test.lua`. (PRD 4 evaluated it for truco and deliberately did **not** use it — see below.)
- **Envido lives inside trick 1, NOT a separate phase.** The old `CantosState` was **deleted**; machine is now `deal → trick → score`. Rationale (user): "play a card = pass" only makes sense if the envido window and the first trick are the same moment. In `TrickState`: envido is callable while `tricksPlayed==0 and not envidoUsed`, at each side's turn before it plays its first card — mano first, and the **pie can still call after mano's card** (correct reglamento; verified in the flow driver). Opening a call sets `self.canto`; while set, it owns input/render (Quiero/No quiero/raises) and pauses card play; `finishCanto` applies points **immediately**, shows a 1.5s banner, then clears `self.canto` and the *same* `currentPlayer` resumes to play their card. AI envido decisions reuse the 0.6s think guard.
- **No Pass button (user):** to decline envido you just play a card. Human open turn shows Envido/Real/Falta buttons **and** the playable hand; clicking a card plays it (declines). Buttons are mouse+keyboard (`[n]` shortcut), reusing `pointInRect`/`drawButton`.
- **Harness impact:** `driveKeys` in `smoke.lua`/`rotation.lua` now detects TrickState (`cur.tricksPlayed`) and answers an AI call with No quiero (`'2'`), else plays a card (Right+Enter) which also declines the human's own envido. Flow verified in `scratchpad/envido.lua` (human open accept/reject, AI-opens + human raises Real, falta malas, both-play-to-decline, click-driven open).

## PRD 4 — truco

- **Reconciliation (important):** PRD 4 is written around `CantosState` + shared `loop.truco*`, but PRD 3 deleted `CantosState`. So truco, fold, and the envido interrupt all live in `TrickState`, and — since it no longer spans two states — truco is **state-local** (`self.trucoLevel`/`trucoLeader`/`trucoPending`), **not on `loop`**. `HandScoreState` is now truco-agnostic: every path into `score` passes explicit `{winner, points}` (normal trick resolution computes `trucoLevel>0 and trucoLevel or 1`; reject/fold pass their own value), and score just awards them.
- **Architecture decision (user asked for the cleanest):** truco does **not** reuse the `Canto` engine. Canto encodes envido's cumulative-pot + in-response-raise semantics; truco is level-based (2/3/4), persists across tricks, is accept-before-raise, and the accepted level must outlive the negotiation. Forcing it through Canto would use the abstraction's shell not its substance. Truco reuses the *patterns* (buttons, 0.6s AI-think, banner) but keeps its own tiny state. `src/truco_rules.lua` = pure math/gating (`trucoRejectValue`=level-1, `trucoFoldValue`, `availableTrucoCall` gating), tested in `truco_rules_test.lua`. `TrucoAiStub` = call/raise iff holds a brava (tier≤4), respond quiero iff brava-or-won-a-trick, never folds.
- **Interrupt = trivial with two independent slots:** a pending truco is `self.trucoPending`; envido runs on `self.canto`. "Envido primero" (offered only when `canInterruptEnvido`: trick 1, nothing played, envido unused) opens an envido canto while the truco pending just sits there; when envido resolves + banner clears, the still-set `trucoPending` is answered (Quiero/No quiero — no second interrupt, envido now closed). No nesting/stashing.
- **Accepting truco closes envido (user rule):** `canCallEnvido`/`canInterruptEnvido` both require `trucoLevel == 0`. Once a truco is accepted, envido is gone for both sides — the interrupt (before answering the first truco) is the *only* way envido coexists with truco ("envido esta primero", to avoid skipping it). The interrupt is **symmetric**: the human gets an "Envido primero" button; the AI, in `aiRespondTruco`, takes envido first too when it holds a strong envido (`EnvidoAiStub.chooseOpen`) before answering the truco. Both directions verified in `scratchpad/truco.lua`.
- **AI order:** `updateAiTurn` tries truco first (so an AI-mano with a brava opens the hand with truco, making the interrupt reachable), then envido, then a card. Accept only sets the stake (`trucoLevel`, `trucoLeader = accepter`); points land at hand end. Reject/fold end the hand immediately via `machine:change('score', {...})`.
- **Verified:** `scratchpad/truco.lua` (accept sets stake; reject §7 values 1/2/3; fold = level-or-1; normal scoring path awards the stake; full interrupt end-to-end). `truco_rules_test.lua` passes. smoke now shows truco firing (AI opens on bravas, human declines).

## Call messages — AI-only panel (user)

- The old verbose result banner (`self.banner`: "AI win the envido (+4)", "You said quiero") is replaced by `self.aiMessage` — a terse **`AI: <call>`** shown **only for the AI's moves** (opens `AI: Envido`/`AI: Truco`/…, responses `AI: Quiero`/`AI: No quiero`). Human moves are silent (you clicked the button). Rendered in `src/gui/Panel` (reused; `Textbox` skipped — its `update` eats Space/Enter). Centered, `gFonts['medium']`, auto-hides after 1.5s, blocks input while up (same pacing as before); the call buttons are hidden while it shows.
- `showAiMessage(label, afterFn)` set at the AI action sites (`openCanto`/`callTruco` when side=='ai', `aiRespondCanto`/`aiRespondTruco`). `finishCanto`/`resolveTrucoAccept` no longer show anything (human accepts/rejects resolve silently). Truco no-quiero passes `machine:change('score', …)` as the `afterFn` so "AI: No quiero" shows before the hand ends. The trick-outcome status ("You win the trick") is separate (bottom status) and unchanged.
- **Harness note:** headless font stubs now return `getWidth`/`getHeight` (Panel sizing needs them); flow drivers gained `pumpClear(ts)` (warm up a few steps, then pump until dialogs clear and not thinking) before a human action that follows an AI move.

## Addendum — played-card stacking + envido reveal

- **Stacking:** `self.playedStack = {human={}, ai={}}` (per hand, init — survives `startTrick`). `playCard` targets `base + {STACK_DIR[side]*STACK_DX, STACK_DY}` (=12) `* #stack[side]`. **Mirrored fans (fix):** both fanning down-*right* made the AI's 3rd card overlap the human's 1st (AI base x=156.5 + 24 = 180.5, +CARD_W 32.5 → 213 > human base 195). `STACK_DIR = {ai=-1, human=1}` sends the AI's fan down-left and the human's down-right, so the two columns can't cross (AI right edge 165 ≤ human left edge 195) and both stay on screen; Y still descends for both (AI up-left would have run into its own face-down hand at `AI_HAND_Y=8`). `playedXFor(side)` extracted alongside `playedYFor`. Asserted in `scratchpad/stack.lua` (per-side sign + a no-horizontal-overlap/on-screen check). the tween `:finish` appends the settled `{card,x,y,rot}` and nils `anims[side]`. `tryResolve` now keys off a per-trick `self.landed` counter (both cards landed) instead of `anims[*].done` (which we clear). Render draws `playedStack[side]` oldest-first then the in-flight `anims[side]` on top. Verified in `scratchpad/stack.lua` (rigged 1-1-then-decider hand → 3 cards each, offsets +12).
- **Message boxes generalized:** `self.aiMessage`/`aiPanel` → `self.dialogs` (list of `{text, panel}`) + `makeDialog(side, text)` positioned via `SIDE_MSG_Y = {ai=66, human=112}`. All the old `aiMessage` gates (input freeze, buttons-vs-message, status, myTurn) key on `#self.dialogs > 0`; render loops them (supports the two simultaneous reveal boxes).
- **Envido reveal (accept only):** `finishCanto` branches — reject applies points immediately (no showdown, per reglamento); accept runs `revealEnvido`: mano's number box (2s) → pie's `"<n> son mejores"`/`"son buenas"` box (2s) → then `award`. Reveal order is tied to the **mano** role (not who called); `aiRespondCanto` drops its "AI: Quiero" on accept (reveal is the feedback), keeps "AI: No quiero".
- **Envido value snapshot (correctness):** `self.envidoValue = {human, ai}` computed in `init` from the full 3-card hands, because a card can be on the table by the time the pie calls envido (mano declined by playing). `envidoAward(outcome, mano, manoValue, pieValue, humanScore, aiScore)` now takes the snapshot values instead of hands; `canto_test.lua` updated to match.

## PRD 5 — chico / partida

- **One scoring funnel (§3, the whole point):** `HandLoopState:awardPoints(side, points)` is now the *only* place `humanScore`/`aiScore` change, and it runs `chicoWinner` on every award. Necessary because points land from **two** places — `HandScoreState:enter` (trick/truco) and `TrickState:award` (envido, which can pay out mid-trick-1). A falta envido crossing 30 would have slipped past an end-of-hand-only check. Returns `true` when the chico ended, so `HandScoreState:enter` can `return` instead of arming its next-deal timer.
- **Stale-continuation guard (subtle, would have been a real bug):** aborting mid-hand orphans a `TrickState` with timers still armed (AI think, `resolve`'s 1.0s pause → `machine:change('score', …)`), which would score into the *next* chico. **`Timer.clear()` is not usable here** — we'd be calling it from inside a Timer callback, and `lib/knife/timer.lua` caches `#group` before iterating (`Timer.update`) while `detach` indexes `group[#group]`, so clearing mid-pass crashes twice over. Instead `HandLoopState:changePhase(name, params)` no-ops while `handAborted`, and **all six** `loop.machine:change` sites in the phase states (1 DealState, 4 TrickState, 1 HandScoreState) now go through it. Only `awardPoints` and `ChicoScoreState` call `machine:change` directly — they're the two allowed to move past an abort. A stale `playCard` on the orphan is harmless (`DealState` reassigns `loop.humanHand`/`aiHand` wholesale; nothing renders the orphan's anims).
- **`src/match_rules.lua`** — pure: `chicoWinner` (`>=CHICO_TARGET`, deliberately not `==`, since falta/truco overshoot), `chicosNeeded` (1 / 2), `partidaWinner`. The `2 of 3, 3rd decisive at 1-1` rule needs no special case: 1-1 fails `>= 2`, and whoever takes the 3rd necessarily reaches 2.
- **`ChicoScoreState`** = a 4th *phase* in the loop's machine (not the stack), so it renders through `drawHud` like the others. Holds 2.5s, then either `gStateStack:pop()` + `push(MatchEndState(winner, loop))` or `loop:startNextChico()`.
- **Dealer across the chico boundary (user):** keeps alternating (`startNextChico` flips it and bumps `handNumber`) rather than a fresh coin flip per chico. A real re-cut would re-randomize, but unbroken rotation keeps `scratchpad/rotation.lua`'s "dealer flips every hand" invariant meaningful.
- **`MatchEndState`** reads its tally off the loop once in `init` (the loop is popped, so nothing live is held): `best_of_3` shows chicos, `single_chico` shows the final score. Enter → pop + `push(StartState())`.
- **StartState temp keys (throwaway, PRD 7 replaces):** Enter = `best_of_3`, `s` = `single_chico` — otherwise `single_chico` is unreachable in-game before the menu exists.
- **Harness impact:** `envido`/`truco`/`stack` hand-rolled fake loops gained `awardPoints`/`changePhase` stubs. `smoke.lua` needed a `gStateStack` stub *and* delta-accumulated hand/point counters (scores reset per chico, `handNumber` per match); `rotation.lua` breaks out when a match ends (a new match re-coin-flips the dealer, legitimately not a flip). New `scratchpad/match.lua`: (a) chico resets + deals on, (b) 1-1 → decisive 3rd → `MatchEndState` 2-1, (c) Enter → StartState, (d) single_chico ends at chico 1, (e) mid-hand falta chico — `tricksPlayed` stays 0, no leaked points, and `changePhase` asserted a no-op while aborted.

## Envido call-feedback fixes (user)

- **Interrupt offers all three variants.** `callButtons`' truco-pending branch now emits `Envido`/`Real envido`/`Falta envido` (plain `CALL_LABEL` names, user's pick) from a shared `ENVIDO_OPENS` local, instead of a single `Envido primero`. The `envido-primero` act was **deleted**, not extended: `applyCallButton`'s existing `call:<type>` fallback already does `openCanto('human', type)` with `trucoPending` untouched underneath — identical behavior, one branch less.
- **Announce-before-award (the real bug).** Rule now: *any AI message that precedes a scoring award must own the award in its `afterFn`.* `aiRespondCanto` used to call `finishCanto()` (which awards) and *then* `showAiMessage`, so when the award ended the chico, `awardPoints` swapped the machine out of TrickState and the message rendered nowhere. Traced: human at 28 → "AI: No quiero" shows 1.5s; human at 29 (the +1 crosses 30) → box never appeared, straight to the chico banner. All messaging moved into `finishCanto`, which is the only place that knows both `outcome.kind` and `answerer = canto.responder` (correct after raises — `Canto:raise` keeps `responder` on whoever must answer). `aiRespondTruco` already had this shape; it was the model.
- **Quiero wording follows the mano role** (4 scenarios the user enumerated). The AI speaks only when the AI answers: as **mano** its quiero merges into its declaration (`revealEnvido(outcome, 'Quiero')` → one box `Quiero, 26`, no repeated number, stays up while the pie replies); as **pie** it gets a bare `AI: Quiero` box first, then the normal mano→pie reveal. A human accept is silent (they clicked it). Reject is always `No quiero`, no number. `awardCanto(outcome)` extracted so `finishCanto` and `revealEnvido` share one payout site.
- **`EnvidoAiStub` takes a value, not a hand** (necessary for scenario 2 to be reachable at all): the pie answers *after* the mano may have played, so `envidoValue(loop.aiHand)` was re-deriving from a 2-card hand — an AI mano with 26 who declined by playing would then almost always say No quiero. Both stub functions now take the `self.envidoValue` snapshot.
- **Chico banner centered** (`gFonts['medium']`, mid-screen) instead of `drawHud`'s bottom status line, which was invisible between the reveal and `MatchEndState`'s big title. `HandScoreState`'s `+N` banner is still skipped when that hand ends the chico — the trick outcome already showed during `resolve`'s 1.0s pause, and the chico banner supersedes it.
- **Harness:** `envido.lua` gained (h1)-(h4) asserting the box text *sequence* for all four mano/answerer combinations, and (i) the chico-winning-reject regression (message on screen while the score is still 29, only then the chico). `truco.lua` checks all three interrupt buttons are offered and that picking Falta opens a ceiling canto with the truco still pending. Reject-path scenarios needed `pumpClear` (the +1.5s announcement) instead of a fixed `pump`. **`match.lua` case (e) was fixed:** it rigged hands without letting `HandLoopState`'s opening `DealState` timer fire, so ~0.5s in that timer built a *second* TrickState and the assertions ran against an orphan — it now pumps ~1.2s before rigging (and needed a longer bound for the extra quiero beat).
- **Every message box names its speaker** (`speaker(side)` -> `'AI: '`/`'You: '`, used by `showAiMessage` *and* both `revealEnvido` boxes). Reported as "AI says quiero and immediately calls 26" when the AI was pie — but the trace showed the sequencing was already right; the middle box was the *player's own* declaration, rendered bare. Only `showAiMessage` had been prefixing, so a lone `26` appearing right after `AI: Quiero` read as the AI still talking (both boxes are centered and only 46px apart). Now: `AI: Quiero` -> `You: 26` -> `AI: 33 son mejores`. Widest box lands at x=251 of 384, so no clipping.
- **The interrupt now DISCARDS the truco (user rule, supersedes the "two independent slots" note above).** `openCanto` clears `self.trucoPending`: answering a truco with any envido variant takes that truco off the table for good — no quiero is owed once the envido ends, play simply resumes, and either side may call truco afresh (`availableTrucoCall(0, nil, side, nil)` → 2). This is *simpler* than the old design, not more complex: the second slot is cleared instead of resumed, so nothing has to be stashed or re-entered. No special turn handling — `callTruco` never moves `currentPlayer`, so the truco caller is still on turn when the envido finishes. No loop: `envidoUsed` is one-shot, so a re-called truco must be answered for real. Observed end to end: `AI: Truco` → human picks Envido (pending nil immediately) → `AI: No quiero` (+1 human) → AI re-calls truco 0.6s later as a *new* pending, now with only Quiero/No quiero offered.

## PRD 6 — heuristic AI

- **Reconciliation — signatures had to grow.** PRD §2/§9 promises "no call-site changes", but the old signatures physically can't carry what §4-§6 need: `chooseCard(hand)` can't see the opponent's card or the trick score, `wantsToCall(hand)` can't see `tricksPlayed`/`trucoLevel`, `chooseResponse(value)` can't express a raise, and **fold is an entirely new decision point** nothing was asking about. Each entry point now takes a `ctx` table built by one place, `TrickState:aiContext()` (`against, myWins, theirWins, tricksPlayed, trucoLevel, myScore, theirScore`). One table rather than positional args is what actually delivers §2's intent — later PRDs add fields without reshaping call sites. Filenames kept per §9, so `*Stub` is now a misnomer.
- **`src/ai_config.lua`** — `{ aggression = 0.5, bluffRate = 0.10, random = math.random }` + `chance`/`coin`/`bluffDelta`/`shiftTier`. **Aggression and bluffing are the same primitive**: both just step a tier along an ordered ladder (`shiftTier`), which is what lets §5's numeric bands and §6's categorical positions share one knob. `random` is an injectable seam purely so tests can kill the coin flips (`AiConfig.random = function() return 0.9 end`); production leaves it alone. This is the one file PRD 7 edits per tournament round.
- **`AiStub` (§4) is deterministic** — §4 defines no bluffing, and a randomized card play reads as a blunder, not deception. Responding: weakest winner → weakest tie → weakest overall. **Leading extends §4's table with the parda case:** §4 only names "either side has a trick win", but by `isHandDecided` a parda plus any single win also settles the hand, so `tricksPlayed - myWins - theirWins >= 1` counts as decisive too — leading small into a decider would throw it away.
- **`EnvidoAiStub` (§5):** `tier(value, aggression)` is pure/public, boundaries `20/27/31` shifted down by `(aggression-0.5)*8` (0 → 24/31/35, 1 → 16/23/27). Exceptional opens **Falta when `wantsFalta` = leading and past 15** (`faltaEnvidoValue` pays `30 - leader` there, so winning it closes the chico), else a coin flip between Real and Falta — the user's rule. `chooseResponse` takes the canto's `availableRaises()` so it can only name a legal escalation; **the ladder's own narrowing is what bounds escalation** (once falta is down the list is empty → quiero/no quiero only), so "raise once" needs no counter.
- **`TrucoAiStub` (§6):** `position(hand, ctx)` → weak/uncertain/strong, then aggression (±1 at ≥0.75 / ≤0.25) and the bluff shift it. `wantsToFold` needs `trucoLevel > 0` — folding with nothing staked would concede 1 point for nothing. **Truco raising needed no new mechanism:** `resolveTrucoAccept` already makes the AI `trucoLeader`, so `availableTrucoCall` offers the next level on its turn and `updateAiTurn`'s existing truco branch takes it.
- **`updateAiTurn` reordered (user; supersedes the PRD 4 "truco first" note):** a Real/Falta-worthy envido now goes **first**, because accepting a truco closes the envido window — trucoing on a big envido throws it away. Ordering keys off `chooseOpen`'s *returned call* (asked exactly once, since a second call would re-roll the bluff and could disagree). A weak envido still lets truco open the hand, which is what keeps the human's "envido primero" interrupt reachable. Then truco → plain envido → fold → card.
- **`aiRespondCanto` gained a raise branch** (`canto:raise` + `showAiMessage`) — safe under the announce-before-award rule since a raise pays nothing. The fold announces first and owns the phase change in its `afterFn` (`'Me voy al mazo'`), same as a truco no quiero.
- **Verified in play, not just in units.** Instrumented soak runs over the real `HandLoopState` confirmed each §7 manual check: 21 covers with **0 overspends**, bravas held at 0-0, strongest led on deciders, envido quiero/no quiero/raise-to-Real/raise-to-Falta all occurring, and **7 folds against 34 "held on"** (so it folds selectively). Two soak zeros were driver artifacts worth remembering: the fold path is unreachable unless the *human* calls truco (an AI-called truco implies the AI was strong, and `myWins > 0` pins it strong forever), and `chooseResponse` only runs when the human opens the envido.
- **Driver re-rigging:** `envido.lua` hands were picked against the old ≥27/≥24 thresholds and now land in different tiers — several were re-rigged to **decent (20-26)**, the only tier that plainly accepts without raising (`copas_4/copas_2/bastos_5` = 26). Both flow drivers set `AiConfig.bluffRate = 0`; `smoke`/`rotation` deliberately leave bluffing live. **Scenario (h2) became unreachable as written** (a decent AI-mano now opens envido itself instead of declining by playing) and was re-routed through the interrupt: an AI-mano with a brava + envido 26 opens *truco*, the human answers Envido, and the AI's merged `Quiero, 26` follows. New cases: envido (j) the AI raising, and two truco fold cases (folds at a stake, plays on without one). smoke 44 → 41 hands (folds end hands earlier).
